# Extension ideas

* [ ] Backend
    - I want a concept of a "user" with auth, so a "game" can persist beyond a single session, with memory etc. This will require a backend. 
    - Users can save lists of "friends" or something like that
* [ ] Multi-user game so you don't have to pass the phone around
* [ ] Allow for user to input a list of custom topics from a csv,  so you can base the game around inside 
jokes 
* [ ] "inifinite" list of topics, where you give a topic and a single LLM call is made to generate a new secret, so you could ask for e.g. famous footballers without having to manually input a list yourself
* [ ] Playing open-source traitors-style music?