- `APP_NAME` (used in resource naming, must be globally unique for S3)
- `BUCKET_NAME` (optional override; defaults from `APP_NAME`)
- `AWS_PROFILE` and `AWS_REGION` (match your profile and `eu-west-1`)


APP_NAME=traitors-game # used in resource naming, must be globally unique for S3
BUCKET_NAME=traitors-game-s3 # optional override; defaults from `APP_NAME`
DISTRIBUTION_ID= # get after running ./deploy/setup.sh 
AWS_PROFILE=deploy # name, can be whatever
AWS_REGION=eu-west-1

AWS_ACCESS_KEY_ID= # after creating profile
AWS_SECRET_ACCESS_KEY= # after creating profile

